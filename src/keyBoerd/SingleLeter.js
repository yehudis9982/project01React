const SingleLeter=({single})=>{
    return<>
    <span  style={{color:single.color,background:single.background,fontFamily:single.font,fontSize:`${single.fontSize}px` ,fontWeight:single.fontWeight ,textDecoration:single.textDecoration}}>{single.letter}</span>
    </>
    }
    export default SingleLeter
   